# Complete numbered-equation truth for two frozen papers

Issue #131 publishes complete O3 inventories for 2404.17771v2 (seven numbered
equations) and 1911.08525v2 (48). K1 V5 contains eleven papers and sixty numbered
equations, including V4's five. Each of the nine prior paper records is exactly
equal in every JSON field, including provenance. All 73 V1–V4 implementation,
configuration and truth files remain byte-identical and replay successfully.

The separate numbered-only codec grants O3 for these two complete inventories.
It preserves every other original/source role, full source metadata, unknowns,
missing proposals and all fourteen frozen candidate/failure dispositions unscored.
The original 1911 section reference without a primary owner remains explicitly
unresolved; its possible independent owner is retained without an automatic join.
The legacy O3–O7 overlay contract remains unchanged.

Both original annotations, assignment and producer history, current and historical
source/native identities, whole displayed bodies and numbers, finite source scope,
and distinct construction review are bound. All 55 manual displays survive:
50 exact automatic source occurrences and five reviewed contained occurrences
within complete displays. Source positions count Unicode code points; native
positions count UTF-16 units. The original 1911 Latin-1 member and UTF-8 snippet
hashes remain distinct. Corrupted native text is membership evidence with
`semantic_quote_truth: false`, not reconstructed linear or mathematical truth.

Finite whole-source review retains all six 2404 and thirteen 1911 automatic guard
diagnostics. It binds the complete style and actual definitions, invocations and
arguments without declaring arbitrary styles safe. Changed source, styles, guards
or metadata cannot inherit that clearance. No deposited source executes, and the
2109 BibTeX failure remains excluded. The twenty-module V5 source inventory,
constructions, actual release and activation each received separate review.

## Actual publication and measurement

The real manual CLI attached both bundles. The actual bounded publisher ran one
history and eleven paper workers in 16.734 seconds, with 14.418 worker CPU seconds
and peak worker RSS 174,544 KiB. It used the current annotation worker throughout.
These figures do not measure aggregate process-tree RSS or 500-paper performance.
The object/bibliography roots are 22,405/18,434 bytes; complete children total
1,991,722 bytes. The receipt enumerates release-root provenance changes.

The collector retains eleven decision children. Nine existing visual papers
produce the same complete predictions and fifty outcomes. The two new O3-only
papers have explicit `not_in_visual_cohort` decisions. Recomputing all historical
cohorts from these predictions gives 117 equal outcomes across eighteen overlapping
paper rows; fresh isolated V1–V4 and pinned V5 replay also pass.

| Cohort | Outcomes | O1 before and after | O2 before and after |
|---|---:|---:|---:|
| V1 | 15 | 1 | 0.9107793204006069 |
| V2 | 23 | 1 | 0.9169720168893188 |
| V3 | 29 | 1 | 0.9176618036504631 |
| V4 / V5 visual cohort | 50 | 0.98989898989899 | 0.8910021250829322 |

The current visual result remains 49 TP / 0 FP / 1 FN, two zero IoUs and no unknown
truth regions. Deltas are zero; targets .90/.75 and baseline history are unchanged.
O3 has a sixty-equation truth denominator, with no new detector score.

## Corrections, validation and limits

Independent source review found that paired whitespace-only bodies could pass.
The fix requires nonempty non-whitespace membership while preserving corrupted
non-whitespace evidence. Real attachment then exposed four final raw-byte checks
incorrectly passed through JSON canonicalization. Direct byte comparisons fix
that boundary; filesystem success and all four late-mutation controls pass.
Both fixes and their original failures have distinct retained reviews.

The first collector stopped at existing visual ordinal seven with an allocation
failure after seven decisions, before publishing metrics. Its exact request and
binary subsequently succeeded under both 768 MiB and 1.5 GiB limits, producing
identical bytes. The complete retry passed under the original 768 MiB limit and
same binary in 80.526 seconds. The initial cause remains unresolved; no resource
limit, runtime, detector or matching code changed. All failed decisions, requests,
outputs and resource/build receipts remain retained.

After ordinary main integration at `ff33385`, formatting, strict all-target and
all-feature Clippy, owning `eval objects --check` and `eval all --check` pass.
Isolated G5 runs 428 Rust / 627 Python / 85 Node tests in 25.789 seconds, with a
separate network namespace, only loopback, failed network probe and absent model
CLIs. Existing local TypeScript supplies Node tests without downloads.

The unchanged collector requires a real owning worktree target; this bounded
build-location deviation was authorized with a six-GiB allowance and monitored
twenty-GiB floor. Other builds remain external. Exact CLI, bridge, Cargo/G5 logs
and generated artifacts are archived before cleanup. The isolated subset scorecard
stays external; the committed global scorecard and previously measured O30 history
are preserved.

[Machine evidence](../../eval/evidence/numbered-math-131.json) binds the complete
reviews, releases, measurements, comparisons and failures. External/model calls
and billed cost are zero; agent reasoning cost is unmetered. The approximately
500 defensible paper target remains open. Eleven admitted papers do not imply
all-metric completeness. Existing formal positives remain; #132 separately owns
the next positive formal construction. Phase exits and system acceptance remain open.
