# Native command diagnostic boundary (#118)

Only the pdftotext call in source_index::build receives NativeDiagnostics. The existing bounded_command API still selects Strict, including callers named pdftotext: the program name never grants a larger budget. OCR, graphics trace and the separate graphics-mask command path retain their previous policy.

Both policies keep at most 65,536 original stderr bytes. NativeDiagnostics drains at most 1,048,576 bytes to EOF using one fixed 8,192-byte scratch buffer. Every read is bounded by the remaining drainage allowance plus one. Observing byte 1,048,577 fails immediately, with bytes_observed explicitly a lower bound (complete=false). At EOF the count is exact (complete=true). The count is not a total-emitted estimate, and no full-stream hash is claimed.

The private CommandOutput carries stdout and typed Diagnostics. Successful native diagnostics emit one tracing warning with the exact retained prefix ASCII-escaped, its byte length, observed count, truncated flag, drainage limit and completeness. Escaping preserves invalid UTF-8 and control bytes without terminal control sequences. The 64 KiB prefix therefore has at most 256 KiB of escaped text. No diagnostics are inserted into reading text, page gaps, confidence, geometry or cached reading-index schema. The k1_index helper installs a fixed warning subscriber writing to stderr, separate from its JSON stdout, so the actual recovery receipt can retain that sink.

Nonzero exits remain CommandFailed. Native failure logging preserves the escaped prefix; the existing display error remains trimmed text, augmented with count/truncation metadata only when truncation occurs. Over-drain native failure reports its prefix through the same sink and bounded metadata in the Task error. Strict overflow retains its previous error string. Stdout keeps its original limit+1 rejection, 48 MiB at the native call. Reads run concurrently; owned kill_on_drop spans both drains and status waiting. The production timeout remains 35 seconds. No retry, quiet option, alternate extractor, schema bump, native parsing relaxation or cache invalidation is added.

The original single-paper diagnostic and all successful indexes are immutable. A future real recovery must use the exact reviewed source and compiled helper against only arXiv2106.09069v1/PaperId5ad6cdda040648c1, record the missing-index precondition, preserve all existing successful bytes and registry rows, validate actual production native pages/tokens/gaps, and archive executable/source/log/time/RSS identities. The original diagnostic XML is evidence of a possible recovery, not an accepted native index. Parent owns any queue continuation generation after a separately reviewed recovery.

Clean baseline at722b6b24 passed: own CLI build94.821954s, fresh objects check.296650s, O1=1 and O2=.9169720168893188 with23 cases. No native or collector execution occurred. All13 focused command tests, formatting and strict all-target/all-feature Clippy passed offline in47.444s total. The targeted receipt is `~/.cache/lysilogy/native-diagnostics-118/targeted-development-v1/receipt.json` (SHA256 `56b291e1fd9f2627fd77a5dbd1c8dcf4a39a3e4b8791e08fbf135c55f3f06cc0`). Actual recovery, complete final gates and current metric refresh remain pending separate review.
