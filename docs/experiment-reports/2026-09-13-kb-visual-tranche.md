# Limited visual tranche — E8.3 / issue #115

This release adds the independently reconciled 2409.03655v1 visual and formal-negative
cohorts to immutable `k1-limited-v3`. It contains four papers and 29 figure/table
regions. Actual O1 is 1.0 (29 TP, 0 FP, 0 FN); O2 is 0.9169720168893188 over all
29 annotated regions, including one historical zero. Every prior paper outcome,
prediction and native/graphics commitment remains exact. The approximately 500-paper
requirement in #97 remains open; this bounded release closes #115 only.

## Construction and scope

The fixed cs.LG/2024 selection was chosen from completed independent reconciliation,
before detector outputs were inspected. Both original annotators first viewed all
six original PDF pages, then used allowlisted source/native exports. Their original
inventories and receipts remain immutable. Producer identities are grounded in
retained Git assignment records and a separate root identity review, rather than
inferred from distinct strings. Root was separate from both producers and performed
the construction adjudication. The independent code/release reviewer was an original
annotator; that role is explicit and is not represented as construction adjudication.

The reusable visual tranche codec validates the original schemas, exhaustive source
roles and actual review history. It handles original 96-dpi rectangles without
rounding them to a new labeling grid. Four figures and two tables retain complete
captions and source/native memberships. Table1 explicitly combines 734 nonwhitespace
UTF-16 core characters, 304 explanatory-note characters and four markers: 1,042 in
total. Four notes, four superscript markers and seven row callouts retain separate
ownership. No empty math supplement was manufactured. All 16 primary and nine
independent informal-role candidates remain visible beside the complete negatives
for numbered equations, formal statements, proofs and algorithms/listings.

Twelve local references are classified as nine visual references and three section
references. The separate external evaluation-plan subsection remains unknown.
Thirty bibliography entries, 90 fields and 33 citation groups/35 target pairs remain
evidence; the new paper supplies no O8–O11 eligibility. All three prior compact paper
records and bibliography records are unchanged. The 34 historical v1/v2 files are
byte-identical. V3 has its own configuration, fixed fifteen-module verifier,
manifest and payload pins, independently reviewed before activation.

Current source inventory is separate from the historical542 candidate and from what
the original annotators saw. The ordinary #109 integration added exactly 411 nodes
(one coverage member and 410 supplementary entry-provenance members) without changing
any existing object/link/role value. Root's fresh delta review checked the new parser
bindings and source roles and explicitly reused its earlier full-page inspection;
it did not claim to have re-viewed those images. The later #118 native-command merge
left all fifteen Python derivation modules unchanged, so the full current run was
not repeated merely for Rust/docs integration.

## Current full-population diagnostic and immutable publication

The genuine current generation at `6f49e55` attempted all original 1,000 identities in
the original order, retaining the original 999-success native map. It produced 724
parsed candidates and 276 explicit errors, zero raw accepted metric cohorts and
zero automatic O1–O11 admissions. Its 940.162525-second run used 498,032 KiB peak child
RSS. The independent audit rehashed 4,456 artifacts, recomputed all candidate/ledger
aggregates and all 5,309 semantic counts, and found no orphan or partial outputs.
All 689 prior #109 candidates are byte-identical; 35 successes come from the
previously unexecuted 290. That earlier 710-subset run is not relabeled as full1,000.

The new wrapper directly loads exact source bytes from a bounded, flat fifteen-module
tree with no symlinks/bytecode, pre/post tree checks and import attestation (six loaded,
nine explicitly unused). Historical542 used normal imports and receives no retroactive
attestation. The unchanged 30-second diagnostic alarm covers serialization/accounting;
actual audit independently checked those boundaries. The runtime is trusted local
Python, not an adversarial filesystem race-proof execution claim.

The reviewed v3 configuration uses historical542 plus this genuine current report.
Pure construction/preview, separate configuration review and guarded activation all
reproduced exact bytes. The first preview's inherited issue105 provenance was corrected
in a separately retained preview; final provenance names issue115. Actual activation
used the production `main` function with explicit arguments under direct source loading,
not a subprocess CLI. It took 8.299646 seconds. Three isolated post-publication replays
reproduced v1/v2/v3 exactly. No old release was rewritten.

## Actual detector measurements

| Cohort | Papers | TP / FP / FN | O2 denominator | Median IoU | Unknown regions |
| --- | ---: | --- | ---: | ---: | ---: |
| v1 | 2 | 15 / 0 / 0 | 15 | 0.9107793204006069 | 0 |
| v2 | 3 | 23 / 0 / 0 | 23 | 0.9169720168893188 | 0 |
| v3 | 4 | 29 / 0 / 0 | 29 | 0.9169720168893188 | 0 |

The first three-collector attempt at `71e773a` ran all three production collectors
successfully but the external wrapper rejected v3: its declared 29-region cohort had
only 23 measured values and six unknowns. The legacy collector accepted list regions
but did not understand the verified singleton page/rectangle shape emitted for the
new paper. That failed attempt, all raw outputs, predictions, executables and 46
first-generation files remain preserved. It is not an accepted v3 measurement.

The reviewed correction adapts only v3 singleton shapes before unchanged strict
page/finite/rectangle/IoU checks. Extra keys and boolean/fractional page values are
rejected. A new aggregate guard requires the exact frozen O2 denominator before
publishing any metric input. V1/v2 interpretations and matching/scoring functions
remain unchanged; no labels, detector source or coordinate tolerance changed.
The corrected run at `70eebb5` completed all three cohorts in 83.474023 seconds,
102,080 KiB peak child RSS, using the explicitly selected Cargo example and retained
binary/build logs. Complete older paper records and full prediction commitments are
exact. Both failed and corrected measurement generations are independently bound.

The six new IoUs are 0.9325167028376546, 0.9555950544859751,
0.854843903775702, 0.16254643935124297, 0.18438363161698818 and
0.9409477989815347. Figure3 (trade-off) and Figure4 (t-SNE) remain weak geometry cases.
Their pages4/5 have observed `unsupported_trace` graphics status; the relationship to
their native-region misses needs a separate investigation. [#121](https://github.com/tjmisko/Lysilogy/issues/121) retains
this evidence and unchanged denominators. No aggregate baseline reset is required:
both values remain exact, and the O1 .90 / O2 .75 targets are unchanged.

## Validation, deviations and retained evidence

The own-CLI before check at `aa64d24` passed (94.793-second build, .285-second objects
check), with exact Cargo-selected CLI bytes and logs retained. Historical actual v1/v2
measurements were reused as permitted. Twenty-five focused visual-codec tests cover
schema/history/source substitution, original geometry, attached-note ownership,
complete role/link inventories, current duplicate-label guards, safe paths and grounded
identity. Versioned dispatch/composition/release tests and the collector's four new
singleton/denominator tests extend those checks. All 41 collector tests passed in the
independent direct-source replay. Final gate results: formatting, strict all-target/all-feature Clippy, all-target Rust, 328 LaTeX tests,
41 focused collector tests, own-CLI objects/bibliography/scale checks, isolated G5
(381 Rust / 537 Python / 85 Node) and all three isolated truth replays passed.
The first gate attempt retained a real G5 environment failure: the worktree lacked
its locked TypeScript5.9.3 package. Its exact existing local package was copied and
hashed offline, then only affected G5 and the pending replays ran again, with all
source/CLI bytes unchanged (77.046003 seconds first attempt, 39.213404 seconds retry).

Ordinary main merges replace the phases file's generic rebase instruction under the
standing no-rebase policy. This deliberately limited manual release and the new
version-scoped metric adapter are documented in the design/contract. Preparation and
wrapper mistakes are retained, including the rejected mismatched gate-head argument
before any command ran. No metric target or hard gate was relaxed, and the original
unavailable cohorts remain unavailable. Agent reasoning cost is unknown; all recorded
external provider/model calls and external cost are zero. Local MuPDF graphics
inspection is part of the bounded collector; no native reading indexes were rebuilt.

Machine evidence: `eval/evidence/visual-tranche.json`; complete per-paper comparison:
`eval/evidence/visual-tranche-cohort-comparison.json`; rejected first attempt:
`eval/evidence/visual-tranche-first-attempt.json`. External raw artifacts, fixed inputs,
portable binaries/source copies and review receipts are retained beneath
`~/.cache/lysilogy/k1-coverage/visual-tranche-115/`. Final independent PR review remains
required before root-controlled merge. The five further fixed pilot papers belong to
#117 and are not admitted by this release.
