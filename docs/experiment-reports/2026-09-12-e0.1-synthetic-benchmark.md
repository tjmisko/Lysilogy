# E0.1 synthetic benchmark tooling and pending 10k baseline

The generator and benchmark runner are implemented and verified against the real catalog,
extractor, artifact store, and running browser app. The required **10,000-paper baseline is
unavailable**: `python3 scripts/bench/run.py --count 10000` failed before generating any PDF with
`[Errno 30] Read-only file system: /home/tjmisko/.cache/lysilogy`. The designated vault was not
relocated. Issue #19 remains a draft until writable external storage permits the complete run.

Source measured: `46db5fb84ec997b5f6b9a86e928b5d681bf39c6d`, including content identity #20 and
the isolated-linker correction #79. [Instructions and methodology](../../scripts/bench/README.md)
describe the reproducible full command. A successful 10k run retains its own artifacts, publishes
only O25/O26 observations, and writes a separate dated Markdown result report.

## Functional measurements only: eight-paper fixture

`python3 scripts/bench/smoke.py` built a debug backend and production frontend, generated exactly
eight small PDFs, and drove the actual isolated app with Playwright Chromium 151.0.7922.34.
All eight real extraction artifacts were populated, three fresh browser contexts rendered the
library, and sixty deterministic title/author/year searches returned independently checked
results. The screenshot was inspected. No external requests, model calls, analysis, imports,
note mutations, or user-library access occurred. Temporary fixture PDFs/artifacts were removed;
derived observations are retained in
[synthetic-benchmark-fixture.json](../../eval/evidence/synthetic-benchmark-fixture.json).

These figures establish functional coverage only. They are not 10k performance estimates and
were not published to the scale collector or used to establish a ratchet baseline.

| Phase | Eight-paper debug observation |
| --- | --- |
| Initial discovered catalog | 0.003768 s |
| Discovered-only no-change scan, median of 3 | 0.001341 s |
| Full extraction + persistence setup | 0.223987 s |
| First populated catalog scan | 0.040535 s |
| Populated no-change scan, median of 3 | 0.020110 s |
| Home first render, median of 3 | 236.0 ms |
| Search p95, 60 queries | 35.8 ms |
| Serial extractor trial, median of 3 | 0.221080 s |
| Four-worker extractor trial, median of 3 | 0.061468 s |
| Experimental extractor efficiency | 0.8992 |

Each extractor trial processed the same eight PDFs and produced the same aggregate output hash.
Four-worker scheduling exists only in the benchmark example. Production ingest remains serial;
**O27 remains unavailable** until E0.5 measures the shipping worker path. Model cost is $0; the
retained raw observations include every phase/trial duration.

## Validation and scorecard delta

- Formatting, Clippy with all targets/features and warnings denied, and 279 Rust tests pass
  (265 library, 12 integration, 2 benchmark example) after the provider lock correction.
- Nineteen offline benchmark Python tests cover determinism, Poppler validity, safe resume,
  per-phase reporting, truth tampering, unavailable small runs, collector composition, and
  no-follow descriptor publication under controlled parent-directory swaps.
- Frontend typecheck, lint, build, and the independently counted query test pass.
- `lysilogy eval tests --check` passes G5 with 279 Rust, 69 Python, and 85 Node tests in an
  isolated network namespace, only loopback present, and no model CLIs on PATH.
- Refreshed the independent provider collector, then `lysilogy eval scale --check` passes:
  O30 remains 0 violations / 10,000 references. Before/after scorecard is unchanged at one of
  five gates passing and one of thirty objectives at target. O25, both O26 components, and
  O27 remain unavailable. No hard gate or objective target was lowered; no measurement miss
  has been disguised as an unavailable value.

A cold G5 run initially exposed the missing Mold linker alias; that foundation correction was
split into #79 / PR #81 and merged independently. A later provider cooldown restart failure
was reproduced as an inherited file-lock lifetime defect and resolved separately in #83 / PR #84.
The benchmark branch integrates that independently cleared fix and the documented O27 sequencing
correction. Fresh scale/tests evaluations pass at source
`e042f3373dce644b5404e863c1e4c4541a87c7d4`; the retained evidence has a separate integration receipt.
The browser fixture remains the original measured observation above, without reattesting it as
a new run. Benchmark implementation code did not change after independent review at `164e1c5`.

## Next action

Once `~/.cache/lysilogy/` is writable, run the default 10k command without lowering its count.
It measures both empty-artifact and fully populated scans; O25 uses the latter. At least 5 GiB
free is required before starting. Run the scale/test checks, retain the generated full-size
report, and review the resulting O25/O26 baseline and any objective misses before completing
#19. The later arXiv scale acceptance remains separate.
