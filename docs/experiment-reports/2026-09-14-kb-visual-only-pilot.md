# Fixed visual-only pilot — E8.3 / issue #117

Immutable `k1-limited-v4` now contains nine papers and all 50 visual regions. Actual
O1 is **0.8979591836734694** (44 TP, 4 FP, 6 FN), below the unchanged .90 target;
O2 is **0.8826424973982893**, including eight zeros and no unknown regions. The
reviewed cohort baseline transition preserves this result and the original failed
ratchet check. Only **2/30 objectives are at target**; system acceptance is not met.

This bounded release adds only O1/O2 eligibility for the five remaining papers in the
original seven-paper manual pilot. It preserves the four prior complete compact
paper records and all 53 v1/v2/v3 files. The published release contains nine papers and 50
figures/tables. Issue #97 and its approximately 500-paper requirement remain open.

## Construction and retained scope

The fixed order is 2310.04162v1 (8 figures, 5 tables), 2001.05217v1 (4 figures),
2002.03492v1 (no figures/tables), 2207.03024v1 (3 figures), and 2303.07834v2
(1 table). No detector predictions selected, replaced or revised these papers.
Original page-first annotations, corrections and independent body reconciliations
remain immutable. Root separately confirmed producer identities from actual retained
Git assignment history and adjudicated all five construction bundles. The original
independent annotator reviews code/publication separately; that work is not presented
as distinct construction adjudication.

The separate evaluation-only codec binds every source occurrence, paired complete
caption/native membership, original page/body rectangle and retained role ledger.
The TableIII full body includes its separately owned note: all 40 native note tokens
have complete page geometry inside the body and do not overlap the caption.
The included Figure3 source in 2207 uses the literal parent/dependency triplet;
its authored caption values remain unchanged. In 2303, the framed equation and the
nonrendered figure source are explicit exclusions with original evidence. The finite
source checks establish the reviewed empty wrapper after a paired TableI boundary;
they do not implement general TeX execution or infer rendered absence from a name.

All original/current formal, equation, proof, algorithm, reference, citation and
bibliography records remain unscored. Original role, ownership, target and ambiguity
variants are copied exactly; 1,513 additive raw-role fields were independently
checked. The two extra independent unheaded proof discussions remain visible.
No new O3–O11 eligibility or formal negatives are manufactured. All old O3–O11
cohorts and denominators remain unchanged.

Independent public-validator probes exposed paired-equation/residual-float,
macro redefinition/composition/argument-ownership and table-note geometry gaps.
Each rejected source generation and its exact probes is retained. The final narrow
guards reject unsupported consumption/assignment contexts and preserve the actual
paired-table predecessor. Forty focused codec tests and the independent adversarial
replays pass at 4cb74a5. No parser, detector or native implementation changes belong
to this issue; detector4 arrives through the ordinary #121 main integration.

## Immutable preview and automatic-history attribution

The genuine prior full1000 run at 6f49e55 remains 724 parsed candidates, 276 explicit
errors and zero automatic admissions. Its original six loaded modules and nine unused
modules stay attested as historical execution. The existing validator proves the
executed closure unchanged except for builder fingerprint registration; a separate
continuity review checked this. No new1000 execution is claimed. Historical542
receives no retroactive import isolation claim.

The pure preview binds all five distinct root reviews through new manifests and
reassembles nine papers under exact committed source bytes. It retains sixteen fixed
modules, exact old four paper/bibliography records, 21 new visual objects and all
unscored roles. The objects payload is 991,418 bytes under the unchanged 1,048,576-byte
verifier cap (57,158 bytes spare). Further truth growth needs a separately reviewed
bounded layout/cap decision; record omission is not a capacity workaround.

The separately reviewed activation at `4cb74a5` reproduced the exact preview in
11.970541 seconds; publication commit `aa49f6e` adds twenty immutable files and
changes only the current v4 verifier pin outside that bundle. Four isolated replays
reproduced all v1/v2/v3/v4 payloads and their 15/23/29/50 visual denominators in
29.471686 seconds. All 53 historical files remain byte-identical. The collector
explicitly selects v4 and applies the existing strict singleton-region interpretation
to that version; matching rules, IoU arithmetic and the complete-denominator guard
remain unchanged.

## Actual measurements

The original afcd7e9 own-CLI baseline is retained: a 93.525657-second Cargo build and
0.333402-second objects check passed. It predates detector4. Legacy outcome comparison
for this issue uses the post-#121 detector4 evidence, so the incoming detector fixes
are not attributed to the visual-only release.

| Cohort | Papers | TP / FP / FN | O1 | O2 / complete denominator |
| --- | ---: | --- | ---: | --- |
| v1 | 2 | 15 / 0 / 0 | 1.0 | 0.9107793204006069 / 15 |
| v2 | 3 | 23 / 0 / 0 | 1.0 | 0.9169720168893188 / 23 |
| v3 | 4 | 29 / 0 / 0 | 1.0 | 0.9169720168893188 / 29 |
| v4 | 9 | 44 / 4 / 6 | 0.8979591836734694 | 0.8826424973982893 / 50 |

The four explicitly selected collectors ran at `2e7e3ad` in 154.236605 seconds,
278,528 KiB peak child RSS. The independent audit rehashed 678 artifacts and
recomputed all 117 repeated-cohort outcomes from frozen truth/native/prediction
bytes, with exact equality for every caption decision and IoU. All complete old
paper records, predictions and native/graphics commitments remain exact against
post-#121 detector4. No new native reading indexes were built. Local bounded MuPDF
graphics inspection is part of collection and is distinct from external model calls.

The new 2310 paper has eight matched figures, four table false positives and five
table misses. TableI–IV predictions contain only the short label, excluding their
separate title paragraph; unchanged caption-overlap matching rejects them. TableV
has no prediction. In 2207, Figure3 has no prediction: its native paragraph interleaves
caption lines with neighboring prose, so widening one anchor would not preserve exact
caption membership. Root separately inspected these native boundaries and three
original pages. [#125](https://github.com/tjmisko/Lysilogy/issues/125), linked to E1/#24,
tracks conservative complete table-caption joining and separate disjoint-caption
representation. Region deficiencies remain separate; #123 tracks vector support.
There is no detector adjustment in #117.

All 50 IoUs are retained in `eval/evidence/k1-v4-cohort-comparison.json`. The eight
zeros comprise six unmatched objects, 2310 Figure8's missing body region and the
historical timeToEvent missing region. New-only median IoU is .2885956631865363
across all 21 new objects; matched-only median .8910021250829322 is a diagnostic,
not O2. The no-visual 2002 paper is retained with zero predictions.

The unadjusted own-CLI objects check exited1 for both objective baseline regressions.
Its raw result and original baselines remain immutable. The separately reviewed
`eval/justifications/k1-v4-cohort.json` establishes the changed denominator and exact
old-paper continuity. Root approved the existing CLI transition: it appends exactly
two O1/O2 history entries and leaves all other baselines and prior history unchanged.
A subsequent normal objects check passes with byte-stable baselines. Both commands
took .586109 seconds together. This is a comparison-basis change, not a target waiver:
O1 .90, O2 .75 and all hard gates remain unchanged; O1 remains below target and #125
is the scoped follow-up. Only O2 and O30 are at target (2/30).

## Validation and deviations

Formatting, strict all-target/all-feature Clippy, all-target Rust and the own-CLI
Cargo-selected build passed at `2e7e3ad` in 61.422402 seconds. The retained binary
and all 205 measured source fingerprints remain exact through later documentation,
metric-evidence and baseline-only commits. Forty focused codec tests, 371 LaTeX
suite tests and 42 collector tests pass. Remaining integrated gates at `ce109ff`
passed in 24.847019 seconds: synthetic provider budget, own-CLI bibliography/scale
and isolated G5, with **386 Rust / 581 Python / 85 Node** tests. G5's sixteen commands
all exit0 under a network namespace containing only loopback and with model CLIs
absent. The owning objects check passes after the reviewed cohort transition above;
all four isolated truth replays are exact. Unavailable G1–G4 and other metrics remain
unavailable; one available hard gate passing is not system acceptance.
The exact cached TypeScript5.9.3 fixture package was copied and rehashed offline
before G5; no package-network call was made. A synthetic activation-guard test's
summary filename collided with its negative receipt case after all eight rejection
checks passed. The original attempt is retained; a separate test generation fixed
only that output name and reproduced all eight fail-before-publication checks.

Ordinary main merges follow the standing no-rebase policy; the deviation from the
phases file's generic rebase instruction is explicit. Agent reasoning cost is unknown;
recorded external model/provider calls and external cost are zero. Bounded
MuPDF inspection by the collector is recorded separately from source-only
construction. No native reading-index rebuild is part of this release.

The first transition invocation used an absolute justification path and was rejected
by the existing safe-relative-path guard before evaluation or mutation. Its logs and
wrapper assertion failure remain intact; the corrected generation changes only that
argument to the same approved relative file. An unlaunched measurement preparation
incorrectly classified mutable active inputs as immutable output checks; the separate
v2 freezer corrects those checks. A rejected gate-head argument also stopped before
any command. These records are retained alongside all passed executions. G5-generated
bytecode was archived before removal; no tracked source was changed by cleanup.

Machine evidence is `eval/evidence/visual-only-pilot.json`; it binds portable binaries,
source copies, original inputs, raw gates, preserved failures, all construction and
independent reviews, and the actual cohort transition. External artifacts are retained
under `~/.cache/lysilogy/k1-coverage/visual-only-117/`. Independent final PR review
remains required before root-controlled merge. This release closes only #117; #97,
its approximately 500-paper requirement, the capacity decision and detector follow-ups
remain open.
